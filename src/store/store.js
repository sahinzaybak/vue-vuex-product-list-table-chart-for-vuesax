import service from "../services/services";

const state = {

}

const mutations = {

}

const actions = {
    fetchTableList({ commit }) {
       console.log("okey")
    },
}

const getters = {}

export default {
    state,
    mutations,
    actions,
    getters
};
